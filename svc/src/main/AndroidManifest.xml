<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="net.callmeike.android.services.svc"
    android:sharedUserId="net.callmeike.android.services.common"
    >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        >
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <service
            android:name=".CookieService"
            android:enabled="true"
            android:exported="true"
            />

        <service
            android:name=".SlowService"
            android:enabled="true"
            android:exported="true"
            />

        <service
            android:name="net.callmeike.android.services.common.LocalService1"
            android:enabled="true"
            android:exported="true"
            android:process="services.common"
            />

        <service
            android:name="net.callmeike.android.services.common.LocalService2"
            android:enabled="true"
            android:exported="true"
            android:process="services.common"
            />
    </application>

</manifest>
